// JS dành riêng cho trang Giỏ hàng (WooCommerce Cart Page)
document.addEventListener('DOMContentLoaded', () => {
    console.log('Cart Page Script Loaded!');
    // Thêm logic tương tác giỏ hàng nếu cần (ví dụ: cập nhật số lượng tự động bằng Ajax)
});
